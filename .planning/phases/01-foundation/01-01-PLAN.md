---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - tsconfig.json
  - .env.local
  - .env.example
  - .gitignore
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/lib/fonts.ts
autonomous: true
requirements:
  - FOUN-01
  - FOUN-02
  - FOUN-03
  - FOUN-04
  - FOUN-05
  - FOUN-06
  - DESN-03
  - DESN-05
  - SEOP-07
  - SEOP-09

must_haves:
  truths:
    - "next build completes without errors"
    - "No fonts.googleapis.com requests appear in Network tab"
    - "Brand color utilities (bg-teal-sage, text-charcoal, bg-warm-gray) work in Tailwind"
    - "Typography classes (type-title, type-heading, type-subheading, type-body) render with correct font families and sizes"
    - "Cloudinary remotePatterns configured so images from res.cloudinary.com are allowed"
    - "All fonts use font-display: swap (via next/font automatic behavior)"
  artifacts:
    - path: "src/lib/fonts.ts"
      provides: "All font definitions in one file"
      exports: ["libreBaskerville", "montserrat", "arapey", "apparel", "destiny"]
    - path: "src/app/globals.css"
      provides: "Tailwind @theme with brand colors + typography hierarchy classes"
      contains: "@theme"
    - path: "src/app/layout.tsx"
      provides: "Root layout applying all font CSS variables to <html>"
      contains: "libreBaskerville.variable"
    - path: "next.config.ts"
      provides: "Cloudinary remotePatterns configuration"
      contains: "res.cloudinary.com"
    - path: ".env.example"
      provides: "Documented environment variables"
      contains: "NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME"
  key_links:
    - from: "src/lib/fonts.ts"
      to: "src/app/layout.tsx"
      via: "import and className application"
      pattern: "import.*from.*@/lib/fonts"
    - from: "src/app/globals.css"
      to: "src/app/layout.tsx"
      via: "CSS import"
      pattern: "import.*globals.css"
    - from: "src/app/globals.css"
      to: "src/lib/fonts.ts"
      via: "CSS variable references matching font variable names"
      pattern: "var\\(--font-libre-baskerville\\)"
---

<objective>
Bootstrap the Next.js project and configure all foundation infrastructure: Tailwind v4 with brand color tokens, all five font families via next/font, Cloudinary remotePatterns, and the base typography hierarchy CSS.

Purpose: Every subsequent phase depends on these configurations being correct. Getting fonts, colors, Cloudinary, and typography wrong here means expensive retroactive fixes later.

Output: A buildable Next.js 16 project with brand tokens, font definitions, and Cloudinary config. No pages beyond a minimal home stub.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@CLAUDE.md
@design-spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Next.js project and install dependencies</name>
  <files>
    next.config.ts, tsconfig.json, .env.local, .env.example, .gitignore, package.json
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md (installation commands, version numbers, project structure)
    .planning/phases/01-foundation/01-CONTEXT.md (locked decisions D-01 through D-09)
    CLAUDE.md (technical guidelines section)
  </read_first>
  <action>
    1. Run `npx create-next-app@latest . --typescript --tailwind --app --turbopack --yes` from the project root directory (per D-01). The project root already exists with planning files, so bootstrap into it. If create-next-app refuses to run in a non-empty directory, use a temp directory and move files.

    2. Pin Tailwind to ^4.2.0 (per D-03) and install next-cloudinary (per D-09):
       ```
       npm install tailwindcss@^4.2.0
       npm install next-cloudinary
       ```

    3. Verify tsconfig.json has strict mode enabled (per D-02). If not, set:
       ```json
       "strict": true
       ```

    4. Replace the contents of `next.config.ts` with Cloudinary remotePatterns (per D-09):
       ```typescript
       import type { NextConfig } from 'next'

       const nextConfig: NextConfig = {
         images: {
           remotePatterns: [
             {
               protocol: 'https',
               hostname: 'res.cloudinary.com',
               pathname: '/**',
             },
           ],
         },
       }

       export default nextConfig
       ```

    5. Create `.env.local` with:
       ```
       NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME="jennieslade"
       ```

    6. Create `.env.example` with:
       ```
       # Cloudinary cloud name (required for image delivery)
       NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME="your-cloudinary-cloud-name"
       ```

    7. Ensure `.gitignore` includes `.env.local` (create-next-app should handle this, but verify).

    8. Create the directory structure:
       ```
       mkdir -p src/components/images
       mkdir -p src/lib
       mkdir -p src/data
       mkdir -p public/fonts
       ```
  </action>
  <verify>
    <automated>cd /Users/Jennie/Documents/Websites/jennie-slade-photo && node -e "const pkg = require('./package.json'); const next = pkg.dependencies.next || pkg.devDependencies.next; const tw = pkg.dependencies.tailwindcss || pkg.devDependencies.tailwindcss; const cld = pkg.dependencies['next-cloudinary']; console.log('next:', next, 'tw:', tw, 'cld:', cld); if (!next || !tw || !cld) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "next" in dependencies or devDependencies
    - package.json contains "tailwindcss" with version starting with "^4.2" or "4.2"
    - package.json contains "next-cloudinary" in dependencies
    - next.config.ts contains the string "res.cloudinary.com"
    - next.config.ts contains the string "remotePatterns"
    - .env.example contains "NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME"
    - .env.local contains "NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME"
    - tsconfig.json contains "strict": true
    - Directories exist: src/lib/, src/components/images/, src/data/, public/fonts/
  </acceptance_criteria>
  <done>Next.js 16 project bootstrapped with TypeScript, Tailwind v4.2+, next-cloudinary installed, Cloudinary remotePatterns configured, env files created, directory structure ready.</done>
</task>

<task type="auto">
  <name>Task 2: Configure fonts, brand colors, typography hierarchy, and root layout</name>
  <files>
    src/lib/fonts.ts, src/app/globals.css, src/app/layout.tsx, src/app/page.tsx, public/fonts/apparel-regular.woff2, public/fonts/destiny-webfont.woff2
  </files>
  <read_first>
    src/app/globals.css (current state after create-next-app)
    src/app/layout.tsx (current state after create-next-app)
    .planning/phases/01-foundation/01-RESEARCH.md (Pattern 1: font definitions, Pattern 2: @theme config, exact code examples)
    CLAUDE.md (Design System section: colors, typography table, font sourcing)
    design-spec.md (brand colors, type styles)
  </read_first>
  <action>
    1. Create placeholder font files so next/font/local does not crash the build (per D-05). Create minimal valid WOFF2 stubs:
       - `public/fonts/apparel-regular.woff2` — create a minimal placeholder (can be a 0-byte file or a simple stub; if next/font crashes on empty file, use a try/catch approach in fonts.ts instead)
       - `public/fonts/destiny-webfont.woff2` — same approach

       **Important note about font stubs:** If `next/font/local` crashes on empty/invalid .woff2 files, instead configure fonts.ts to conditionally load local fonts only when files exist, OR use a known-good minimal WOFF2 binary. The research notes that missing files cause build warnings but not crashes — verify this. If it does crash, wrap in a try/catch and export a fallback font config.

    2. Create `src/lib/fonts.ts` with ALL font definitions (per D-04, D-05, D-06):
       ```typescript
       import { Libre_Baskerville, Montserrat, Arapey } from 'next/font/google'
       import localFont from 'next/font/local'

       export const libreBaskerville = Libre_Baskerville({
         subsets: ['latin'],
         weight: ['400', '700'],
         style: ['normal', 'italic'],
         variable: '--font-libre-baskerville',
         display: 'swap',
       })

       export const montserrat = Montserrat({
         subsets: ['latin'],
         weight: ['400', '500', '600'],
         variable: '--font-montserrat',
         display: 'swap',
       })

       export const arapey = Arapey({
         subsets: ['latin'],
         weight: ['400'],
         style: ['normal', 'italic'],
         variable: '--font-arapey',
         display: 'swap',
       })

       // Apparel — premium font from Fort Foundry
       // Falls back to Georgia until Jennie provides the real .woff2 file
       // Path is relative to THIS file (src/lib/fonts.ts)
       export const apparel = localFont({
         src: '../../public/fonts/apparel-regular.woff2',
         variable: '--font-apparel',
         display: 'swap',
         fallback: ['Georgia', 'serif'],
       })

       // Destiny — custom script font
       // Falls back to cursive until Jennie provides the real .woff2 file
       export const destiny = localFont({
         src: '../../public/fonts/destiny-webfont.woff2',
         variable: '--font-destiny',
         display: 'swap',
         fallback: ['cursive'],
       })
       ```

       **If local font loading crashes with stub files:** Remove the localFont imports and instead create CSS-only fallback variables. Define `--font-apparel` and `--font-destiny` directly in globals.css pointing to Georgia and cursive respectively. Document the swap-in procedure for when real files arrive.

    3. Replace `src/app/globals.css` entirely with (per D-07, D-08, D-12):
       ```css
       @import "tailwindcss";

       @theme {
         /* Brand colors — warm undertone, never cool (per D-07, D-08) */
         --color-black: #000000;
         --color-charcoal: #1a1a1a;
         --color-gray: #6b6b6b;
         --color-blue-mist: #b8c5d4;
         --color-teal-sage: #5f8f8b;
         --color-warm-gray: #d4d1cb;
         --color-warm-gray-light: #e3e0da;
         --color-off-white: #f0eeeb;
         --color-white: #ffffff;

         /* Font family tokens — reference CSS variables set by next/font on <html> */
         --font-display: var(--font-libre-baskerville), Georgia, serif;
         --font-heading: var(--font-montserrat), sans-serif;
         --font-subheading: var(--font-apparel), Georgia, sans-serif;
         --font-body: var(--font-libre-baskerville), Georgia, serif;
         --font-accent: var(--font-arapey), serif;
         --font-script: var(--font-destiny), cursive;
       }

       /* Typography hierarchy — matches design-spec.md exactly (per D-12) */
       @layer base {
         .type-title {
           font-family: var(--font-display);
           font-size: 30px;
           color: var(--color-black);
           text-transform: uppercase;
           letter-spacing: 0.02em;
           line-height: 1.2;
         }

         .type-heading {
           font-family: var(--font-heading);
           font-size: 12px;
           color: var(--color-charcoal);
           text-transform: uppercase;
           letter-spacing: 0.02em;
           line-height: 1.8;
         }

         .type-subheading {
           font-family: var(--font-subheading);
           font-size: 15px;
           color: var(--color-charcoal);
           text-transform: uppercase;
           letter-spacing: 0.07em;
           line-height: 1.1;
         }

         .type-body {
           font-family: var(--font-body);
           font-size: 16px;
           color: var(--color-charcoal);
           letter-spacing: 0.02em;
           line-height: 1.9;
         }

         .type-accent {
           font-family: var(--font-accent);
           font-size: 16px;
           color: var(--color-charcoal);
         }

         body {
           font-family: var(--font-body);
           color: var(--color-charcoal);
           background-color: var(--color-white);
         }
       }
       ```

    4. Replace `src/app/layout.tsx` with root layout that applies all font variables (per Pattern 1 from research):
       ```tsx
       import type { Metadata } from 'next'
       import { libreBaskerville, montserrat, arapey, apparel, destiny } from '@/lib/fonts'
       import './globals.css'

       export const metadata: Metadata = {
         title: 'Jennie Slade Photography',
         description: 'Las Vegas portrait photographer specializing in families, weddings, and senior portraits. Over 20 years of capturing memories.',
       }

       export default function RootLayout({
         children,
       }: {
         children: React.ReactNode
       }) {
         return (
           <html
             lang="en"
             className={[
               libreBaskerville.variable,
               montserrat.variable,
               arapey.variable,
               apparel.variable,
               destiny.variable,
             ].join(' ')}
           >
             <body>{children}</body>
           </html>
         )
       }
       ```

    5. Replace `src/app/page.tsx` with a minimal placeholder home page:
       ```tsx
       export default function Home() {
         return (
           <main className="min-h-screen flex items-center justify-center bg-off-white">
             <div className="text-center space-y-4">
               <h1 className="type-title">Jennie Slade Photography</h1>
               <p className="type-body text-gray">Site under construction</p>
             </div>
           </main>
         )
       }
       ```

    6. Run `next build` to verify everything compiles. Fix any errors before completing.
  </action>
  <verify>
    <automated>cd /Users/Jennie/Documents/Websites/jennie-slade-photo && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/fonts.ts exports libreBaskerville, montserrat, arapey, apparel, destiny
    - src/lib/fonts.ts contains "display: 'swap'" for every font definition
    - src/lib/fonts.ts contains "variable: '--font-libre-baskerville'"
    - src/lib/fonts.ts contains "variable: '--font-montserrat'"
    - src/lib/fonts.ts contains "variable: '--font-arapey'"
    - src/lib/fonts.ts contains "variable: '--font-apparel'"
    - src/lib/fonts.ts contains "variable: '--font-destiny'"
    - src/app/globals.css contains "@import \"tailwindcss\""
    - src/app/globals.css contains "@theme {"
    - src/app/globals.css contains "--color-teal-sage: #5f8f8b"
    - src/app/globals.css contains "--color-warm-gray: #d4d1cb"
    - src/app/globals.css contains "--color-charcoal: #1a1a1a"
    - src/app/globals.css contains "--color-off-white: #f0eeeb"
    - src/app/globals.css contains ".type-title"
    - src/app/globals.css contains ".type-heading"
    - src/app/globals.css contains ".type-subheading"
    - src/app/globals.css contains ".type-body"
    - src/app/globals.css contains "line-height: 1.9"
    - src/app/globals.css contains "letter-spacing: 0.07em" (subheading)
    - src/app/layout.tsx contains "import.*from.*@/lib/fonts"
    - src/app/layout.tsx contains "libreBaskerville.variable"
    - src/app/layout.tsx contains "montserrat.variable"
    - src/app/layout.tsx contains "apparel.variable"
    - src/app/layout.tsx contains "destiny.variable"
    - `next build` exits with code 0
  </acceptance_criteria>
  <done>All five fonts configured via next/font with swap display, brand color palette available as Tailwind utilities, typography hierarchy classes match design-spec.md, root layout wires everything together, project builds successfully.</done>
</task>

</tasks>

<verification>
1. `next build` completes without errors
2. grep for "fonts.googleapis.com" in build output returns nothing (fonts are self-hosted by next/font)
3. globals.css contains all 9 brand colors in @theme block
4. globals.css contains all 5 typography classes (type-title, type-heading, type-subheading, type-body, type-accent)
5. layout.tsx applies all 5 font CSS variables to html element
6. next.config.ts has remotePatterns for res.cloudinary.com
</verification>

<success_criteria>
- The project builds cleanly with `next build`
- Brand colors are available as Tailwind utilities (bg-teal-sage, text-charcoal, bg-warm-gray, etc.)
- Typography hierarchy matches the design-spec exactly (sizes, fonts, letter-spacing, line-height)
- No external font requests to fonts.googleapis.com
- Cloudinary images from res.cloudinary.com are allowed by remotePatterns
- All fonts use font-display: swap
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
