---
phase: 04-about-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/about/page.tsx
autonomous: true
requirements:
  - ABOU-01
  - ABOU-02
  - ABOU-03
  - ABOU-04
  - ABOU-05

must_haves:
  truths:
    - "Visitor reads Jennie's personal story (grandpa's darkroom, first daughter, grew into a business)"
    - "Visitor sees a photo placeholder of Jennie at the top of the page"
    - "Visitor reads about the generational continuity (20+ years, same families, babies to seniors)"
    - "Visitor sees at least one testimonial quote placeholder styled as an editorial callout"
    - "Visitor sees a clear CTA button linking to /contact at the bottom"
    - "All sections below the photo fade in on scroll"
  artifacts:
    - path: "src/app/about/page.tsx"
      provides: "Complete About page with bio, photo placeholder, testimonials, CTA"
      min_lines: 80
      contains: "generateMetadata"
  key_links:
    - from: "src/app/about/page.tsx"
      to: "/contact"
      via: "Button href"
      pattern: "href.*contact"
    - from: "src/app/about/page.tsx"
      to: "src/components/ui/ScrollFade.tsx"
      via: "import ScrollFade"
      pattern: "import ScrollFade"
    - from: "src/app/about/page.tsx"
      to: "src/components/layout/Section.tsx"
      via: "import Section"
      pattern: "import Section"
---

<objective>
Build the About page that tells Jennie's story with editorial warmth, integrates a photo placeholder, testimonial placeholder callouts, and a contact CTA. This is a single content page using existing design system components.

Purpose: Visitors understand who Jennie is, feel her warmth and experience, and are moved toward getting in touch.
Output: Complete `/about` page at `src/app/about/page.tsx`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-about-page/04-CONTEXT.md

<interfaces>
<!-- Existing components the executor will use directly -->

From src/components/layout/Section.tsx:
```typescript
type SectionVariant = 'default' | 'muted' | 'warm'
interface SectionProps {
  children: React.ReactNode
  variant?: SectionVariant  // 'default' (white), 'muted' (off-white), 'warm' (warm-gray-light)
  className?: string
  id?: string
}
// Usage: <Section variant="muted"><content /></Section>
// Inner div: max-w-[1200px] mx-auto px-6 md:px-8, py-20
```

From src/components/ui/Button.tsx:
```typescript
type ButtonVariant = 'primary' | 'secondary'
interface ButtonProps {
  variant?: ButtonVariant
  href?: string
  children: React.ReactNode
  className?: string
}
// Usage: <Button href="/contact">Get in Touch</Button>
// Renders as Next.js Link when href provided
```

From src/components/ui/ScrollFade.tsx:
```typescript
// 'use client' component — can be child of Server Component
interface ScrollFadeProps {
  children: React.ReactNode
  className?: string
}
// Usage: <ScrollFade><Section>...</Section></ScrollFade>
// Fades in once when 15% visible, fires once only
```

From src/app/globals.css:
```
Typography classes available:
  .type-title    — Libre Baskerville 30px uppercase (H1)
  .type-heading  — Montserrat 12px uppercase (H2 labels)
  .type-body     — Libre Baskerville 16px, 1.9 line-height (paragraphs)
  .type-accent   — Arapey 16px (testimonial callouts)
  .type-subheading — Apparel 15px uppercase (buttons, labels)
```

From src/components/homepage/WelcomeSection.tsx (voice/pattern reference):
```typescript
// Server Component, no 'use client'
// Pattern: <Section variant="default"> wrapping <div className="max-w-2xl mx-auto text-center">
// Copy voice: first person, warm, story-first, no em dashes
// Short paragraphs with type-body mb-6
// Section label with type-heading mb-8
```

From src/app/page.tsx (page composition pattern):
```typescript
// Server Component wrapping sections in <main>
// ScrollFade wraps sections below the fold
// import pattern: components from @/components/...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the About page with editorial bio, photo placeholder, testimonials, and CTA</name>
  <files>src/app/about/page.tsx</files>
  <read_first>
    - src/components/homepage/WelcomeSection.tsx (voice and styling pattern)
    - src/components/layout/Section.tsx (variant options)
    - src/components/ui/Button.tsx (CTA pattern)
    - src/components/ui/ScrollFade.tsx (scroll animation pattern)
    - src/app/page.tsx (page composition pattern)
    - src/app/globals.css (typography classes, especially type-accent for testimonials)
    - .planning/phases/04-about-page/04-CONTEXT.md (all decisions D-01 through D-17)
    - CLAUDE.md (voice guidelines, design system)
  </read_first>
  <action>
Create `src/app/about/page.tsx` as a Server Component (no 'use client'). This is the complete About page.

**Metadata** (per ABOU-01):
Export a `generateMetadata` function returning:
- title: "About | Jennie Slade Photography"
- description: "Las Vegas portrait photographer with over twenty years of experience capturing families, weddings, and seniors. Meet Jennie and learn her story."

**Page structure** (per D-05, D-06, D-07, D-08):

The page is a `<main>` element containing these sections top to bottom:

**Section 1 — Photo + Intro (NOT wrapped in ScrollFade, immediately visible)**
Use `<Section variant="default">`. Inside, a centered column `max-w-3xl mx-auto text-center`.

Photo placeholder (per D-05, ABOU-02): A warm-gray div simulating a portrait photo of Jennie.
```jsx
<div className="mx-auto w-full max-w-md aspect-[3/4] bg-warm-gray rounded-sm mb-12" aria-label="Photo of Jennie Slade — coming soon" />
```

Page title (per D-08 step 2):
```jsx
<h1 className="type-title mb-4">Hello, I&apos;m Jennie</h1>
```

Decorative subtitle:
```jsx
<p className="type-heading mb-10">Las Vegas Portrait Photographer</p>
```

**Section 2 — Origin Story (ScrollFade wrapped)**
Use `<Section variant="default">`. Inside, `max-w-2xl mx-auto text-center`.

Section label:
```jsx
<h2 className="type-heading mb-8">How It All Started</h2>
```

Origin story paragraphs (per D-03, D-09, D-10, ABOU-01). Write in first person, warm, conversational. No em dashes. Short paragraphs. The copy MUST include these story beats:
- Grandpa taught her photography. He had a developing darkroom in his bathroom.
- When she had her first daughter, she wanted beautiful photos of her.
- She got a good camera, practiced, and people started asking her to take their photos.
- It grew from there into a twenty-year career.

Actual copy to use:

Paragraph 1:
"Photography has been part of my life for as long as I can remember. My grandpa was the one who taught me. He had a developing darkroom set up in his bathroom, and I thought it was the coolest thing in the world. Watching a photo appear on paper felt like magic."

Paragraph 2:
"When my first daughter was born, I knew I wanted beautiful photos of her. Not just snapshots, but real portraits that captured who she was. So I got a good camera, practiced constantly, and fell completely in love with it all over again."

Paragraph 3:
"Then something funny happened. Friends started asking me to take photos of their kids. Then their friends asked. And their friends. Before I knew it, I had a business. That was over twenty years ago, and I still can't believe I get to do this for a living."

Each paragraph: `<p className="type-body mb-6">`. Use `&apos;` for apostrophes.

**Section 3 — What Working With Me Is Like (ScrollFade wrapped)**
Use `<Section variant="muted">`. Inside, `max-w-2xl mx-auto text-center`.

Section label:
```jsx
<h2 className="type-heading mb-8">What to Expect</h2>
```

Copy (per D-08 step 4). Write about fun, relaxed, stress-free sessions:

Paragraph 1:
"Here's what I want you to know. Sessions with me are not stiff or formal. There's no awkward posing or forced smiles. I'm the photographer who gets down on the ground with your toddler, cracks jokes with your teenager, and makes your husband forget he didn't want to be there."

Paragraph 2:
"My favorite photos are the ones where you forgot I was even shooting. The belly laugh. The little hand reaching up. The look between you and your partner that says everything. Those are the moments worth keeping."

**Section 4 — Generational Story (ScrollFade wrapped)**
Use `<Section variant="default">`. Inside, `max-w-2xl mx-auto text-center`.

Section label:
```jsx
<h2 className="type-heading mb-8">Twenty Years of Your Stories</h2>
```

Copy (per D-04, ABOU-03):

Paragraph 1:
"This is the part that still gets me. I've been photographing some of the same families for over eighteen years. The tiny newborn I cradled for her first portrait? She's a senior now. Some of those babies are getting married. A few are even having babies of their own."

Paragraph 2:
"There is nothing in this world like watching a family grow up through my lens. Every session is a new chapter, and I'm honored to be the one who gets to write it down in photos."

**Section 5 — Testimonial Placeholder 1 (ScrollFade wrapped)**
Use `<Section variant="warm">`. Inside, `max-w-2xl mx-auto text-center`.

Testimonial callout (per D-11, D-12, D-13, ABOU-04):
```jsx
<blockquote className="type-accent text-[20px] italic leading-relaxed text-gray">
  <span className="text-[48px] leading-none text-warm-gray block mb-2">&ldquo;</span>
  Your testimonial will appear here.
</blockquote>
```

**Section 6 — Personal Touch (ScrollFade wrapped)**
Use `<Section variant="default">`. Inside, `max-w-2xl mx-auto text-center`.

Section label:
```jsx
<h2 className="type-heading mb-8">A Little More About Me</h2>
```

Copy (per D-01, D-06):

Paragraph 1:
"When I'm not behind the camera, I'm a mom of five. My kids range from 13 to 25, and one of them is married now. I've lived every stage of family life that I photograph, from sleepless newborn nights to watching your baby walk across a graduation stage."

Paragraph 2:
"I'm a Las Vegas native, born and raised. This city is home, and so are the families who live here. I love that I get to be part of your story."

**Section 7 — Testimonial Placeholder 2 (ScrollFade wrapped)**
Use `<Section variant="muted">`. Inside, `max-w-2xl mx-auto text-center`.

Same testimonial blockquote pattern as Section 5 (per D-12).

**Section 8 — CTA (ScrollFade wrapped)**
Use `<Section variant="default">`. Inside, `max-w-2xl mx-auto text-center`.

CTA copy (per D-14, D-15, ABOU-05):
```jsx
<h2 className="type-heading mb-6">Let&apos;s Work Together</h2>
<p className="type-body mb-8">
  I&apos;d love to hear your story. Whether it&apos;s a growing family, a wedding day, or a senior
  ready to take on the world, let&apos;s chat about capturing it.
</p>
<Button href="/contact">Get in Touch</Button>
```

**ScrollFade wrapping pattern** (per D-16):
Section 1 (photo + intro) is NOT wrapped in ScrollFade (immediately visible).
Sections 2 through 8 are each wrapped in `<ScrollFade>`.

**Imports needed:**
```typescript
import Section from '@/components/layout/Section'
import Button from '@/components/ui/Button'
import ScrollFade from '@/components/ui/ScrollFade'
import type { Metadata } from 'next'
```

Use `export const metadata: Metadata = { ... }` (static export, not generateMetadata function, since there are no dynamic params).

**Code style:**
- Server Component (no 'use client')
- Well-commented like WelcomeSection.tsx
- Use `&apos;` for apostrophes, `&ldquo;` and `&rdquo;` for curly quotes
  </action>
  <verify>
    <automated>cd /Users/Jennie/Documents/Websites/jennie-slade-photo && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/app/about/page.tsx
    - Contains `export const metadata` with title "About | Jennie Slade Photography"
    - Contains `h1` with text "Hello, I'm Jennie" (using &apos;)
    - Contains the word "darkroom" (grandpa's origin story, per D-03)
    - Contains the phrase "twenty years" (generational continuity, per D-04/ABOU-03)
    - Contains "mother" or "mom" and "five" (personal details, per D-01)
    - Contains two `blockquote` elements (testimonial placeholders, per D-12)
    - Contains `Button` with `href="/contact"` (CTA, per D-14/ABOU-05)
    - Contains `ScrollFade` imports and usage wrapping sections below the photo
    - Contains `Section` imports with at least one variant="muted" and one variant="warm"
    - Contains warm-gray photo placeholder div with `aspect-[3/4]` and `bg-warm-gray`
    - No 'use client' directive (Server Component)
    - No em dashes anywhere in the copy
    - `next build` completes without errors
  </acceptance_criteria>
  <done>
    The About page renders at /about with Jennie's complete personal story, a warm-gray photo placeholder, two testimonial placeholder callouts, and a CTA button linking to /contact. All sections below the photo use ScrollFade animations. The build succeeds.
  </done>
</task>

</tasks>

<verification>
1. `next build` completes without errors
2. Visiting `/about` in the browser shows the editorial About page
3. Photo placeholder visible at top, warm-gray, portrait orientation
4. Bio copy reads naturally in first person, tells the darkroom origin story
5. Two testimonial placeholder callouts appear with italic Arapey styling
6. CTA button at bottom links to /contact
7. Sections fade in on scroll (ScrollFade working)
8. Page looks good on mobile (text readable, photo full-width)
</verification>

<success_criteria>
- /about renders a complete editorial About page with Jennie's story
- Photo placeholder, bio, testimonials, and CTA are all present
- Copy is warm, first-person, story-first with no em dashes
- Page uses existing Section, Button, and ScrollFade components correctly
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-about-page/04-01-SUMMARY.md`
</output>
