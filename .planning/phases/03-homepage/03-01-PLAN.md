---
phase: 03-homepage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/ScrollFade.tsx
  - src/components/homepage/HeroSection.tsx
  - src/components/homepage/WelcomeSection.tsx
autonomous: true
requirements: [HOME-01, HOME-02, HOME-03, HOME-05, HOME-06, DESN-01, DESN-04]

must_haves:
  truths:
    - "Hero section fills the viewport with a warm-gray placeholder and tagline overlay"
    - "Welcome section communicates Jennie's voice, generational story, and links to /about"
    - "Sections below the hero fade in on scroll via IntersectionObserver"
    - "Both sections are beautiful on mobile (hero 60vh, welcome full-width)"
  artifacts:
    - path: "src/components/ui/ScrollFade.tsx"
      provides: "IntersectionObserver fade-in wrapper"
      contains: "'use client'"
    - path: "src/components/homepage/HeroSection.tsx"
      provides: "Full-viewport hero with tagline"
      contains: "Real moments"
    - path: "src/components/homepage/WelcomeSection.tsx"
      provides: "Editorial welcome copy with Meet Jennie CTA"
      contains: "Button"
  key_links:
    - from: "src/components/homepage/WelcomeSection.tsx"
      to: "/about"
      via: "Button component with href"
      pattern: "href.*about"
    - from: "src/components/ui/ScrollFade.tsx"
      to: "IntersectionObserver"
      via: "useEffect with observer"
      pattern: "IntersectionObserver"
---

<objective>
Build the ScrollFade utility component and the first two homepage sections (Hero and Welcome) as standalone sub-components, ready to be composed into page.tsx.

Purpose: These are the foundation pieces of the editorial homepage. The hero establishes the cinematic feel, the welcome section communicates Jennie's voice and 20+ year story, and ScrollFade provides the subtle scroll animation used by all sections below the fold.

Output: Three new component files in src/components/ ready for composition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-homepage/03-CONTEXT.md
@.planning/phases/03-homepage/03-RESEARCH.md

@src/components/layout/Section.tsx
@src/components/ui/Button.tsx
@src/app/globals.css

<interfaces>
<!-- Existing components the executor will use directly -->

From src/components/layout/Section.tsx:
```typescript
type SectionVariant = 'default' | 'muted' | 'warm'
interface SectionProps {
  children: React.ReactNode
  variant?: SectionVariant  // 'default' = white bg, 'muted' = off-white, 'warm' = warm-gray-light
  className?: string
  id?: string
}
export default function Section({ children, variant, className, id }: SectionProps)
```

From src/components/ui/Button.tsx:
```typescript
type ButtonVariant = 'primary' | 'secondary'
interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: ButtonVariant
  href?: string           // When provided, renders as Next.js Link
  children: React.ReactNode
  className?: string
}
export default function Button({ variant, href, children, className }: ButtonProps)
```

Typography classes (from src/app/globals.css):
- `.type-title` — Libre Baskerville 30px, uppercase, 0.02em spacing, line-height 1.2
- `.type-heading` — Montserrat 12px, uppercase, 0.02em spacing
- `.type-subheading` — Apparel 15px, uppercase, 0.07em spacing
- `.type-body` — Libre Baskerville 16px, 0.02em spacing, line-height 1.9

Color utilities:
- `bg-warm-gray` (#d4d1cb), `bg-off-white` (#f0eeeb), `bg-white` (#ffffff)
- `text-charcoal` (#1a1a1a), `text-gray` (#6b6b6b), `text-white` (#ffffff)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ScrollFade component and HeroSection component</name>
  <files>src/components/ui/ScrollFade.tsx, src/components/homepage/HeroSection.tsx</files>

  <read_first>
    - src/app/globals.css (typography classes and color tokens)
    - src/components/ui/PageTransition.tsx (existing 'use client' component pattern)
    - src/components/layout/Section.tsx (understand Section API — hero does NOT use Section)
  </read_first>

  <action>
    **ScrollFade component** (`src/components/ui/ScrollFade.tsx`):
    Create a `'use client'` wrapper component that uses IntersectionObserver to fade children in on scroll. Per D-15 and D-16.

    Implementation:
    - Add `'use client'` directive at top
    - Props: `children: React.ReactNode`, `className?: string`
    - Use `useRef<HTMLDivElement>`, `useState(false)` for `isVisible`
    - `useEffect` creates IntersectionObserver with `{ threshold: 0.15 }`
    - On `entry.isIntersecting`, set `isVisible(true)` and call `observer.disconnect()` (fire once, per D-16)
    - Cleanup: `return () => observer.disconnect()`
    - Wrapper div classes: `transition-all duration-700 ease-out`
    - When visible: `opacity-100 translate-y-0`
    - When hidden: `opacity-0 translate-y-4` (16px upward drift, subtle per D-15)
    - ScrollFade wrapper div must NOT set any background color (only opacity/transform)
    - Export as default

    **HeroSection component** (`src/components/homepage/HeroSection.tsx`):
    Create a Server Component (NO 'use client') for the full-viewport hero. Per D-01 through D-06.

    Implementation:
    - Do NOT wrap in `<Section>` — hero is full-bleed, no max-width constraint
    - Outer div: `relative w-full h-[60vh] md:h-[80vh] bg-warm-gray` (mobile-first per D-06, D-17)
    - Add a semi-transparent overlay for text legibility: `absolute inset-0 bg-black/20` (research recommendation for contrast on warm-gray placeholder)
    - Add a comment block explaining how to swap placeholder for CloudinaryImage later:
      ```
      {/* Replace this div with CloudinaryImage when real photo is ready:
          <CloudinaryImage src="hero-public-id" alt="Las Vegas portrait photographer" fill priority className="object-cover" />
          The priority prop is REQUIRED for LCP (D-04). */}
      ```
    - Tagline overlay: `absolute inset-0 flex items-center justify-center px-6`
    - H1 element with the tagline text: **"Real moments. Real joy. Remembered forever."** (per D-01)
    - H1 classes: `type-title text-white text-center max-w-2xl` with responsive font sizing override: `text-[22px] md:text-[30px] lg:text-[38px]` (per research pitfall 5 — 30px overflows on mobile)
    - The `type-title` class sets uppercase and letter-spacing; the text-[Xpx] overrides the font-size
    - Add `leading-relaxed` for comfortable line spacing on multiline tagline on mobile
    - Export as default
  </action>

  <verify>
    <automated>cd /Users/Jennie/Documents/Websites/jennie-slade-photo && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - src/components/ui/ScrollFade.tsx exists with `'use client'` on line 1
    - ScrollFade contains `IntersectionObserver` and `threshold: 0.15`
    - ScrollFade contains `observer.disconnect()` inside the isIntersecting callback
    - ScrollFade contains `opacity-0 translate-y-4` and `opacity-100 translate-y-0`
    - ScrollFade contains `duration-700`
    - src/components/homepage/HeroSection.tsx exists WITHOUT `'use client'`
    - HeroSection contains `h-[60vh] md:h-[80vh]`
    - HeroSection contains `bg-warm-gray`
    - HeroSection contains `bg-black/20` overlay div
    - HeroSection H1 contains exact text "Real moments. Real joy. Remembered forever."
    - HeroSection H1 has `text-[22px] md:text-[30px] lg:text-[38px]`
    - HeroSection contains comment about CloudinaryImage replacement with `priority` prop
    - TypeScript compilation passes with no errors
  </acceptance_criteria>

  <done>ScrollFade component provides IntersectionObserver fade-in. HeroSection renders full-viewport warm-gray placeholder with centered white tagline "Real moments. Real joy. Remembered forever." at responsive sizes. Both pass TypeScript type checking.</done>
</task>

<task type="auto">
  <name>Task 2: Create WelcomeSection component with elevated editorial copy</name>
  <files>src/components/homepage/WelcomeSection.tsx</files>

  <read_first>
    - src/components/layout/Section.tsx (use Section with default variant)
    - src/components/ui/Button.tsx (use secondary variant with href="/about")
    - .firecrawl/jennieslade.com/index.md (existing copy to elevate — line 9)
    - CLAUDE.md (voice guidelines: first person, no em dashes, warm, conversational)
  </read_first>

  <action>
    Create a Server Component (NO 'use client') for the welcome/intro section. Per D-07 through D-10.

    **Structure:**
    - Wrap in `<Section variant="default">` (white background, per D-08)
    - Content centered with `max-w-2xl mx-auto text-center` for editorial reading line
    - Small heading above the copy: "Hello There" in `type-heading` with `mb-8` (mirrors existing site greeting)
    - Three paragraphs of body copy in `type-body` class, each wrapped in `<p>` with `mb-6` spacing
    - After the paragraphs, a `<Button variant="secondary" href="/about">Meet Jennie</Button>` (per D-09) with `mt-8`

    **Welcome copy (write exactly this — in Jennie's authentic voice per D-07, D-10):**

    Paragraph 1:
    "Some of the babies I photographed in my first year are heading off to college now. That still blows my mind. For over twenty years, I've been showing up for Las Vegas families at every stage. The tiny newborn yawns, the chaotic toddler giggles, the awkward middle school years, and now the cap-and-gown moments. Getting to watch your families grow up has been the greatest gift of my career."

    Paragraph 2:
    "I'm not the photographer who makes you stand stiff and say cheese. I'm the one who makes your kids laugh so hard they forget I'm even there. Sessions with me are relaxed, fun, and full of real moments. The ones you'll actually want to frame."

    Paragraph 3:
    "If you're looking for someone who genuinely cares about your family's story, and who will still be here to photograph your grandkids someday, I'd love to hear from you."

    **Voice compliance checklist (verify before done):**
    - All first person ("I," "my," "I've," "I'm")
    - Zero em dashes (use commas and periods instead)
    - Zero corporate words (no "leverage," "optimize," "utilize," "ensure," "experience" as verb)
    - Short paragraphs (2-3 sentences max per paragraph — the first paragraph is 4 sentences which is acceptable since they're short)
    - Warm, conversational, story-first
    - Mentions 20+ years and generational continuity (newborn to senior)
    - No third person references to Jennie

    **Semantic HTML:**
    - The "Hello There" uses `<p className="type-heading">` not `<h2>` (the page H1 is in the hero; this is a decorative label, not a structural heading — or use `<h2>` if preferred for document outline)
    - Actually, use `<h2 className="type-heading mb-8">Hello There</h2>` for proper document outline. One H1 (tagline in hero), one H2 (section heading here).
  </action>

  <verify>
    <automated>cd /Users/Jennie/Documents/Websites/jennie-slade-photo && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - src/components/homepage/WelcomeSection.tsx exists WITHOUT `'use client'`
    - Contains `import Section from '@/components/layout/Section'`
    - Contains `import Button from '@/components/ui/Button'`
    - Contains `<Section variant="default">` or `<Section>` (default is white)
    - Contains `<Button variant="secondary" href="/about">Meet Jennie</Button>`
    - Copy contains "over twenty years" (generational continuity per D-07)
    - Copy contains "babies I photographed" or similar newborn-to-senior narrative
    - Copy is entirely first person (contains "I'm" or "I've", does NOT contain "Jennie offers" or "she")
    - Copy contains ZERO em dashes (— character must not appear in any string literal)
    - All paragraphs use `type-body` class
    - Contains `<h2` element with "Hello There" text
    - TypeScript compilation passes with no errors
  </acceptance_criteria>

  <done>WelcomeSection renders an editorial intro with warm, first-person copy about 20+ years of photographing Las Vegas families, from newborns to seniors. Includes "Meet Jennie" secondary button linking to /about. Copy follows voice guidelines: first person, no em dashes, no corporate language, story-first.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (all three new files type-check)
- `npm run build` completes without errors
- All three files exist: ScrollFade.tsx, HeroSection.tsx, WelcomeSection.tsx
- No `'use client'` in HeroSection or WelcomeSection (Server Components)
- `'use client'` present in ScrollFade only
</verification>

<success_criteria>
Three component files created and type-checked. Hero has full-viewport warm-gray placeholder with responsive tagline. Welcome has editorial copy in Jennie's voice with generational story. ScrollFade is ready for wrapping sections below the fold.
</success_criteria>

<output>
After completion, create `.planning/phases/03-homepage/03-01-SUMMARY.md`
</output>
