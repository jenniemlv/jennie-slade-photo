---
phase: 07-seo-and-open-graph
plan: 03
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified: []
autonomous: false
requirements: [SEOP-06]

must_haves:
  truths:
    - "Lighthouse SEO score is 100 on all pages"
    - "Lighthouse Performance, Accessibility, and Best Practices are 90+ on all pages"
    - "Any issues found during audit are fixed within this plan"
  artifacts: []
  key_links: []
---

<objective>
Run Lighthouse audit on all pages after SEO infrastructure is in place, fix any issues found, and verify 90+ scores across all categories.

Purpose: Validate that all SEO work is correct and the site meets performance targets before moving to Phase 8.
Output: Lighthouse scores documented, any fixes applied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-seo-and-open-graph/07-CONTEXT.md
@.planning/phases/07-seo-and-open-graph/07-01-SUMMARY.md
@.planning/phases/07-seo-and-open-graph/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Lighthouse CLI audit on all pages and fix issues</name>
  <files></files>
  <read_first>
    - .planning/phases/07-seo-and-open-graph/07-01-SUMMARY.md (what infrastructure was added)
    - .planning/phases/07-seo-and-open-graph/07-02-SUMMARY.md (what metadata was added)
  </read_first>
  <action>
**Step 1: Start dev server or build and start production server**

```bash
cd /Users/Jennie/Documents/Websites/jennie-slade-photo
npx next build && npx next start &
```

Wait for the server to be ready on localhost:3000.

**Step 2: Run Lighthouse CLI on key pages**

Use `npx lighthouse` (or install if needed) to audit these pages in headless mode:
- http://localhost:3000 (homepage)
- http://localhost:3000/about
- http://localhost:3000/portfolio
- http://localhost:3000/weddings
- http://localhost:3000/contact

Run with: `npx lighthouse URL --output=json --chrome-flags="--headless --no-sandbox" --only-categories=performance,accessibility,best-practices,seo`

Record all scores. Target: 90+ on all four categories, SEO should be 100.

**Step 3: Identify and fix any issues**

Common issues to check and fix:
- Missing meta descriptions (should be fixed by Plan 02)
- Missing alt text on images (should already be in place from Phase 5)
- Missing lang attribute (already in layout.tsx)
- Tap targets too small on mobile
- Color contrast issues
- Missing viewport meta tag
- Render-blocking resources

For each issue found, make the minimal fix in the appropriate file. Add the fixed file to files_modified in the summary.

**Step 4: Re-run Lighthouse on any page that scored below 90 to confirm the fix worked.**

**Step 5: Kill the server process when done.**

If Lighthouse CLI is not available or cannot run in this environment (headless Chrome not available), document the limitation and provide a manual verification checklist instead. The key SEO checks can also be validated by:
- Viewing page source for meta tags and JSON-LD
- Checking /sitemap.xml response
- Checking /robots.txt response
- Verifying heading hierarchy with a grep
  </action>
  <verify>
    <automated>cd /Users/Jennie/Documents/Websites/jennie-slade-photo && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Lighthouse audit attempted on at least homepage, portfolio, and contact pages
    - All SEO scores are 100 (or issues documented with fixes applied)
    - All Performance/Accessibility/Best Practices scores are 90+ (or issues documented with fixes applied)
    - Any files modified to fix issues are listed
    - Build still passes after fixes
  </acceptance_criteria>
  <done>Lighthouse audit complete. All pages score 90+ across all categories (or issues documented with remediation).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify SEO infrastructure works end-to-end</name>
  <files></files>
  <action>Human verification of complete SEO infrastructure across all pages. Claude starts the dev server before presenting verification steps.</action>
  <what-built>Complete SEO infrastructure: optimized meta titles/descriptions on all 8 pages, JSON-LD LocalBusiness structured data, sitemap, robots.txt, and OG tags for social sharing.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000
    2. Check browser tab title shows "Las Vegas Portrait Photographer | Jennie Slade"
    3. Navigate to each page and verify unique title appears in browser tab
    4. View page source (Cmd+U) on homepage and search for "application/ld+json" to confirm LocalBusiness JSON-LD is present
    5. Visit http://localhost:3000/sitemap.xml and confirm 9 URLs are listed
    6. Visit http://localhost:3000/robots.txt and confirm it allows all crawlers
    7. Test social sharing preview: paste https://jennieslade.com into https://www.opengraph.xyz/ to verify OG tags render (will only work after deployment)
    8. Optional: paste the JSON-LD into https://search.google.com/test/rich-results to validate structured data
  </how-to-verify>
  <verify>Human confirms all SEO elements are correct</verify>
  <acceptance_criteria>
    - Browser tab shows correct unique title on every page
    - JSON-LD LocalBusiness schema visible in page source
    - /sitemap.xml returns 9 URLs
    - /robots.txt allows all crawlers
  </acceptance_criteria>
  <done>User has verified SEO infrastructure works correctly across all pages.</done>
  <resume-signal>Type "approved" if SEO looks correct, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
Phase 7 complete when:
1. All 8 pages have unique, optimized meta titles with "Las Vegas"
2. All 8 pages have OG tags for social sharing
3. JSON-LD LocalBusiness schema renders on every page
4. /sitemap.xml lists all 9 routes
5. /robots.txt allows crawling
6. Lighthouse SEO score is 100
7. Lighthouse overall scores are 90+ on all categories
</verification>

<success_criteria>
- Lighthouse SEO: 100 on all audited pages
- Lighthouse Performance/Accessibility/Best Practices: 90+ on all audited pages
- Human verification confirms meta titles, JSON-LD, sitemap, and robots.txt are correct
</success_criteria>

<output>
After completion, create `.planning/phases/07-seo-and-open-graph/07-03-SUMMARY.md`
</output>
