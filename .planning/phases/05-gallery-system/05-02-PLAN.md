---
phase: 05-gallery-system
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/portfolio/page.tsx
  - src/app/weddings/page.tsx
  - src/app/families/page.tsx
  - src/app/seniors/page.tsx
autonomous: false
requirements:
  - PORT-01
  - PORT-04
  - PORT-05
  - PORT-06
must_haves:
  truths:
    - "Portfolio hub page at /portfolio links to /weddings, /families, /seniors with editorial tiles"
    - "Weddings gallery page renders intro text and 12-image grid with aspect-[3/2] landscape"
    - "Families gallery page renders intro text and 12-image grid with aspect-[3/2] landscape"
    - "Seniors gallery page renders intro text and 12-image grid with aspect-[2/3] portrait"
    - "All four pages are accessible and render without layout shift"
  artifacts:
    - path: "src/app/portfolio/page.tsx"
      provides: "Portfolio hub with three editorial category tiles"
      exports: ["default", "metadata"]
    - path: "src/app/weddings/page.tsx"
      provides: "Weddings gallery page with GalleryGrid"
      exports: ["default", "metadata"]
    - path: "src/app/families/page.tsx"
      provides: "Families gallery page with GalleryGrid"
      exports: ["default", "metadata"]
    - path: "src/app/seniors/page.tsx"
      provides: "Seniors gallery page with GalleryGrid"
      exports: ["default", "metadata"]
  key_links:
    - from: "src/app/portfolio/page.tsx"
      to: "/weddings, /families, /seniors"
      via: "Next.js Link component"
      pattern: "href /weddings"
    - from: "src/app/weddings/page.tsx"
      to: "src/components/gallery/GalleryGrid.tsx"
      via: "import GalleryGrid"
      pattern: "import GalleryGrid"
    - from: "src/app/weddings/page.tsx"
      to: "src/data/galleries.ts"
      via: "import weddingImages"
      pattern: "import weddingImages"
---

<objective>
Build the portfolio hub page and three gallery pages that consume the GalleryGrid component and gallery data from Plan 01.

Purpose: These four pages are the visual core of the photography site. The portfolio hub acts as a magazine table of contents, and each gallery page showcases a session type with editorial intro text and the curated image grid.

Output: Four new pages at `/portfolio`, `/weddings`, `/families`, `/seniors` using the existing Section, GalleryGrid, and ScrollFade components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-gallery-system/05-CONTEXT.md
@.planning/phases/05-gallery-system/05-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/data/galleries.ts:
```typescript
export interface GalleryImage {
  id: string
  src: string
  alt: string
}
export const weddingImages: GalleryImage[]   // 12 entries, landscape
export const familyImages: GalleryImage[]    // 12 entries, landscape
export const seniorImages: GalleryImage[]    // 12 entries, portrait
```

From src/components/gallery/GalleryGrid.tsx:
```typescript
interface GalleryGridProps {
  images: GalleryImage[]
  aspectRatio: '3/2' | '2/3'
}
export default function GalleryGrid({ images, aspectRatio }: GalleryGridProps)
// 'use client' component — renders responsive grid + lightbox
```

From src/components/layout/Section.tsx:
```typescript
type SectionVariant = 'default' | 'muted' | 'warm'
export default function Section({ children, variant, className, id }: SectionProps)
```

From src/components/homepage/PortfolioPreview.tsx:
```typescript
// Reference for tile styling: Link with aspect-[3/4], bg-warm-gray, hover overlay, type-subheading label
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create portfolio hub and three gallery pages</name>
  <files>src/app/portfolio/page.tsx, src/app/weddings/page.tsx, src/app/families/page.tsx, src/app/seniors/page.tsx</files>
  <read_first>
    - src/components/homepage/PortfolioPreview.tsx (tile styling reference)
    - src/app/about/page.tsx (page structure reference: header clearance, Section usage, metadata export)
    - src/data/galleries.ts (created in Plan 01: arrays and type)
    - src/components/gallery/GalleryGrid.tsx (created in Plan 01: props interface)
    - .planning/phases/05-gallery-system/05-CONTEXT.md (all D- decisions)
    - CLAUDE.md (voice guidelines for gallery intro copy)
  </read_first>
  <action>
Create four Server Component page files. NONE should have `'use client'` because the client boundary is in GalleryGrid.

**FILE 1: src/app/portfolio/page.tsx** (per D-01 through D-05)

```typescript
import type { Metadata } from 'next'
import Link from 'next/link'
import Section from '@/components/layout/Section'
import ScrollFade from '@/components/ui/ScrollFade'

export const metadata: Metadata = {
  title: 'Portfolio — Jennie Slade Photography',
  description: 'Browse wedding, family, and senior portrait photography by Jennie Slade, Las Vegas photographer with over 20 years of experience.',
}
```

Page structure:
- `<main id="main-content" className="pt-32 md:pt-40">` for header clearance per D-05, D-12
- `<Section>` wrapping the content
- `<h1 className="type-title text-center mb-4">Portfolio</h1>` per D-04
- Optional one-line intro: `<p className="type-body text-center text-gray mb-16 max-w-2xl mx-auto">A look into the moments I love capturing most.</p>`
- Category tiles in a `<div className="flex flex-col gap-8 md:gap-12">` (NOT a 3-col grid, these are large editorial tiles stacked vertically per D-03)

Each tile is a `<Link>` per D-01, D-02:
```tsx
<ScrollFade>
  <Link
    href="/weddings"
    className="group relative block aspect-[16/9] md:aspect-[21/9] bg-warm-gray overflow-hidden rounded-sm"
  >
    {/* Hover overlay */}
    <div className="absolute inset-0 bg-black/0 group-hover:bg-black/10 transition-colors duration-300" />
    {/* Text overlay, bottom-left */}
    <div className="absolute bottom-8 left-8 md:bottom-12 md:left-12">
      <h2 className="type-title text-white mb-2">Weddings</h2>
      <p className="type-body text-white/80">From first look to last dance.</p>
    </div>
  </Link>
</ScrollFade>
```

Use these descriptors (per D-02):
- Weddings: "From first look to last dance."
- Families: "The chaos. The love. All of it."
- Seniors: "A milestone worth remembering."

The tiles use a wider aspect ratio than the homepage tiles (which are aspect-[3/4]). Use aspect-[16/9] md:aspect-[21/9] for a cinematic editorial feel, wider and more dramatic than the homepage preview. This makes the hub feel distinct.

**FILE 2: src/app/weddings/page.tsx** (per D-06, D-07, D-09)

```typescript
import type { Metadata } from 'next'
import Section from '@/components/layout/Section'
import GalleryGrid from '@/components/gallery/GalleryGrid'
import { weddingImages } from '@/data/galleries'

export const metadata: Metadata = {
  title: 'Wedding Photography — Jennie Slade Photography',
  description: 'Las Vegas wedding photography capturing every moment from ceremony to reception. Over 20 years of experience documenting love stories.',
}

export default function WeddingsPage() {
  return (
    <main id="main-content" className="pt-32 md:pt-40">
      <Section>
        <div className="max-w-2xl mx-auto text-center mb-12 md:mb-16">
          <h1 className="type-title mb-6">Weddings</h1>
          <p className="type-body">
            Your wedding day moves fast. The nervous laughter before the ceremony, the tears during the vows, that moment on the dance floor when the whole room is singing along. I love capturing all of it, the big moments and the tiny ones you almost forgot happened.
          </p>
        </div>
        <GalleryGrid images={weddingImages} aspectRatio="3/2" />
      </Section>
    </main>
  )
}
```

Wedding intro copy guidelines (Claude's discretion, follow Jennie's voice): First person ("I love capturing..."), about emotion not logistics, 2-3 sentences max, no em dashes, warm and conversational.

**FILE 3: src/app/families/page.tsx** (per D-06, D-07, D-09)

Same structure as weddings. Import `familyImages`. Use `aspectRatio="3/2"` (landscape per D-09).

Metadata:
- Title: 'Family Photography — Jennie Slade Photography'
- Description: 'Las Vegas family portrait sessions capturing the love, laughter, and connection that make your family uniquely yours.'

Family intro copy (2-3 sentences, Jennie's voice): About freezing the chaos and love. About how fast kids grow. About generational continuity (families she's photographed for years). Example: "Kids grow up so fast. One day they're climbing all over you at a photo session, and the next they're heading off to college. I've been photographing Las Vegas families for over twenty years, and some of my favorite sessions are with families I've known since their kids were newborns."

**FILE 4: src/app/seniors/page.tsx** (per D-06, D-07, D-09)

Same structure. Import `seniorImages`. Use `aspectRatio="2/3"` (portrait per D-09).

Metadata:
- Title: 'Senior Portraits — Jennie Slade Photography'
- Description: 'Las Vegas senior portrait photography marking this milestone with confidence, personality, and style.'

Senior intro copy (2-3 sentences, Jennie's voice): About marking a milestone. About showing who they really are right now. Fun, encouraging tone. Example: "Senior year is kind of a big deal. You've worked hard to get here, and you deserve portraits that show who you are right now, not some stiff yearbook photo. Let's find a spot you love, bring your personality, and have some fun with it."

**ALL pages must:**
- Export `metadata` for SEO
- Use `<main id="main-content">` as the root element
- Have `pt-32 md:pt-40` for header clearance per D-12
- NOT have `'use client'` (they are Server Components)
- Use `<Section>` as the content wrapper
  </action>
  <verify>
    <automated>cd /Users/Jennie/Documents/Websites/jennie-slade-photo && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/portfolio/page.tsx` exists, is a Server Component (no 'use client'), exports metadata
    - Portfolio page has three Link tiles to `/weddings`, `/families`, `/seniors`
    - Each tile has category name in `type-title` and descriptor in `type-body` per D-02
    - `src/app/weddings/page.tsx` imports `GalleryGrid` and `weddingImages`, passes `aspectRatio="3/2"`
    - `src/app/families/page.tsx` imports `GalleryGrid` and `familyImages`, passes `aspectRatio="3/2"`
    - `src/app/seniors/page.tsx` imports `GalleryGrid` and `seniorImages`, passes `aspectRatio="2/3"`
    - All gallery pages have editorial intro text in first person, warm tone, 2-3 sentences
    - All pages have `pt-32 md:pt-40` for header clearance
    - All pages export `metadata` with unique title and description
    - `npm run build` completes without errors
    - No `'use client'` in any of the four page files
  </acceptance_criteria>
  <done>Four pages exist and build successfully: /portfolio hub with editorial tiles, /weddings with landscape grid, /families with landscape grid, /seniors with portrait grid</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of gallery system</name>
  <what-built>
    Complete gallery system: portfolio hub page with three editorial tiles, and three gallery pages (weddings, families, seniors) each with editorial intro and 12-image grid with warm-gray placeholders. Lightbox opens on click with keyboard/swipe navigation.
  </what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Visit http://localhost:3000/portfolio
       - Page title "Portfolio" is visible
       - Three large editorial tiles stack vertically with category names and descriptors
       - Tiles link to /weddings, /families, /seniors
       - Generous spacing between tiles, warm-gray backgrounds
    3. Click "Weddings" tile or visit http://localhost:3000/weddings
       - Editorial intro text appears in Jennie's voice
       - 12 warm-gray placeholder blocks in a grid (3 col desktop, 2 tablet, 1 mobile)
       - All blocks are landscape orientation (wider than tall)
       - Click any block: lightbox opens full-screen with dark overlay
       - Press left/right arrows to navigate, Escape to close
    4. Visit http://localhost:3000/families
       - Same layout as weddings, different intro text
       - 12 landscape placeholder blocks
    5. Visit http://localhost:3000/seniors
       - Same layout, different intro text
       - 12 PORTRAIT placeholder blocks (taller than wide, different from weddings/families)
    6. Test on mobile (resize browser or use phone):
       - Tiles stack to single column
       - Grid goes to 1 column on mobile, 2 on tablet
       - Lightbox swipe works on touch device
    7. Check header: Does not overlap content (pt-32/pt-40 clearance working)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues with layout, spacing, or lightbox behavior</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` passes without errors
2. All four routes are accessible: /portfolio, /weddings, /families, /seniors
3. Portfolio hub links correctly to all three gallery pages
4. Gallery grids render with correct aspect ratios (landscape for weddings/families, portrait for seniors)
5. Lightbox opens on click, keyboard nav works, escape closes
6. No 'use client' in any page file (client boundary is only in GalleryGrid)
7. All pages have unique metadata titles and descriptions
</verification>

<success_criteria>
- Portfolio hub at /portfolio shows three editorial tiles linking to gallery pages
- All three gallery pages render with editorial intro and 12-image grids
- Weddings and families use landscape aspect ratio, seniors uses portrait
- Lightbox opens on any image click with keyboard and swipe navigation
- No layout shift (warm-gray containers hold space before images load)
- Pages are Server Components with client boundary only at GalleryGrid level
- Human approves visual appearance
</success_criteria>

<output>
After completion, create `.planning/phases/05-gallery-system/05-02-SUMMARY.md`
</output>
