---
phase: 02-layout-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/Section.tsx
  - src/components/ui/Button.tsx
  - src/components/ui/PageTransition.tsx
  - src/app/globals.css
  - package.json
autonomous: true
requirements:
  - LAYO-05
  - LAYO-06
  - DESN-06

must_haves:
  truths:
    - "Section component renders three background variants (white, off-white, warm-gray-light)"
    - "Section component constrains content to max-width 1200px with correct padding"
    - "Primary button renders with black background, white text, Apparel font, uppercase, 10px border-radius"
    - "Secondary button renders with transparent background, black border, and inverts on hover"
    - "Page transition fade-in animation plays on route change via CSS keyframe"
  artifacts:
    - path: "src/components/layout/Section.tsx"
      provides: "Reusable section layout primitive with variant prop"
      exports: ["Section"]
    - path: "src/components/ui/Button.tsx"
      provides: "Primary and secondary button components"
      exports: ["Button"]
    - path: "src/components/ui/PageTransition.tsx"
      provides: "CSS fade-in wrapper using usePathname as key"
      exports: ["PageTransition"]
  key_links:
    - from: "src/components/layout/Section.tsx"
      to: "src/app/globals.css"
      via: "Tailwind brand color utilities (bg-off-white, bg-warm-gray-light)"
      pattern: "bg-off-white|bg-warm-gray-light"
    - from: "src/components/ui/Button.tsx"
      to: "src/app/globals.css"
      via: "type-subheading CSS class for Apparel font"
      pattern: "type-subheading"
    - from: "src/components/ui/PageTransition.tsx"
      to: "src/app/globals.css"
      via: "animate-fade-in CSS class"
      pattern: "animate-fade-in"
---

<objective>
Build the three design system primitives (Section, Button, PageTransition) that every subsequent phase consumes. Install lucide-react for icon support used by the Header in Plan 02.

Purpose: These are the atoms of the design system. Section wraps every content block on every page. Button is used for CTAs throughout the site. PageTransition provides the subtle route-change fade. Getting these right first means Plans 02 and 03 (and all future phases) build on solid primitives.

Output: Three component files ready to import, plus the fade-in keyframe animation in globals.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-layout-shell/02-CONTEXT.md
@.planning/phases/02-layout-shell/02-RESEARCH.md
@.planning/phases/02-layout-shell/02-UI-SPEC.md
@src/app/globals.css
@src/app/layout.tsx
@src/lib/fonts.ts

<interfaces>
<!-- Existing type classes from globals.css that components must use -->
From src/app/globals.css:
- .type-title: Libre Baskerville 30px uppercase 0.02em (site name)
- .type-heading: Montserrat 12px uppercase 0.02em (nav links)
- .type-subheading: Apparel 15px uppercase 0.07em (buttons)
- .type-body: Libre Baskerville 16px 1.9 line-height (body text)

<!-- Brand color Tailwind utilities available -->
bg-black, bg-charcoal, bg-off-white (#f0eeeb), bg-warm-gray (#d4d1cb),
bg-warm-gray-light (#e3e0da), bg-white, bg-teal-sage
text-black, text-charcoal, text-gray, text-white, text-teal-sage
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install lucide-react and create Section + Button components</name>
  <files>package.json, src/components/layout/Section.tsx, src/components/ui/Button.tsx</files>
  <read_first>
    - src/app/globals.css (existing type classes and brand color tokens)
    - src/app/layout.tsx (current root layout structure)
    - .planning/phases/02-layout-shell/02-RESEARCH.md (Pattern 5: Section, Pattern 6: Button)
    - .planning/phases/02-layout-shell/02-UI-SPEC.md (Section and Button specs)
  </read_first>
  <action>
1. Install lucide-react (needed by Header in Plan 02, install now to avoid dependency issues):
   ```bash
   npm install lucide-react
   ```

2. Create `src/components/layout/Section.tsx` — Server Component (no 'use client'):
   - Props: `children: React.ReactNode`, `variant?: 'default' | 'muted' | 'warm'` (default: 'default'), `className?: string`, `id?: string`
   - Use a `variantClasses` lookup object (NOT dynamic class construction per Tailwind v4 rule):
     - `default`: `'bg-white'`
     - `muted`: `'bg-off-white'`
     - `warm`: `'bg-warm-gray-light'`
   - Outer `<section>` element gets: variant background class, `py-20` (80px vertical padding per D-19), and the optional `className` and `id` props
   - Inner `<div>` gets: `max-w-[1200px] mx-auto px-6 md:px-8` (mobile: 24px padding, desktop: 32px per D-19)
   - NOTE: Mobile padding is `px-6` (24px), desktop is `px-8` (32px). Mobile-first means `px-6` is the base, `md:px-8` is the breakpoint override.
   - Add a brief JSDoc comment at the top of the file explaining usage and variants
   - Export as default

3. Create `src/components/ui/Button.tsx` — Server Component (no 'use client'):
   - Props: `variant?: 'primary' | 'secondary'` (default: 'primary'), `href?: string`, `children: React.ReactNode`, `className?: string`, `onClick?: () => void`, plus standard HTML button attributes via `React.ButtonHTMLAttributes<HTMLButtonElement>`
   - Use a `variantClasses` lookup object:
     - `primary`: `'bg-black text-white border border-black hover:opacity-80'`
     - `secondary`: `'bg-transparent text-black border border-black hover:bg-black hover:text-white'`
   - Base classes: `type-subheading inline-block px-[14px] py-[10px] rounded-[10px] transition-all duration-150 cursor-pointer`
   - When `href` is provided, render as `<Link href={href}>` from `next/link`. Otherwise render as `<button>`.
   - The `type-subheading` class applies: Apparel font (with Georgia fallback), 15px, uppercase, 0.07em letter spacing per D-17.
   - Hover states per D-17/D-18: Primary gets 80% opacity. Secondary inverts to black bg/white text.
   - Add JSDoc comment explaining primary vs secondary usage
   - Export as default
  </action>
  <verify>
    <automated>cd /Users/Jennie/Documents/Websites/jennie-slade-photo && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `node_modules/lucide-react/package.json` exists
    - `src/components/layout/Section.tsx` exists and contains `export default function Section`
    - `src/components/layout/Section.tsx` contains `bg-off-white` and `bg-warm-gray-light` as complete strings (not dynamic)
    - `src/components/layout/Section.tsx` contains `max-w-[1200px]` and `px-6 md:px-8`
    - `src/components/layout/Section.tsx` contains `py-20`
    - `src/components/layout/Section.tsx` does NOT contain `'use client'`
    - `src/components/ui/Button.tsx` exists and contains `export default function Button`
    - `src/components/ui/Button.tsx` contains `type-subheading`
    - `src/components/ui/Button.tsx` contains `rounded-[10px]` and `px-[14px]` and `py-[10px]`
    - `src/components/ui/Button.tsx` contains `hover:opacity-80` for primary variant
    - `src/components/ui/Button.tsx` contains `hover:bg-black hover:text-white` for secondary variant
    - `src/components/ui/Button.tsx` does NOT contain `'use client'`
    - `src/components/ui/Button.tsx` imports `Link` from `next/link`
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>Section component renders three background variants with correct max-width and padding. Button component renders primary and secondary variants with correct typography, border-radius, and hover states. lucide-react is installed.</done>
</task>

<task type="auto">
  <name>Task 2: Create PageTransition component and add fade-in keyframe to globals.css</name>
  <files>src/components/ui/PageTransition.tsx, src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (to append animation without disrupting existing styles)
    - .planning/phases/02-layout-shell/02-RESEARCH.md (Pattern 4: Page Transition)
  </read_first>
  <action>
1. Add the fade-in keyframe animation to the END of `src/app/globals.css` (after the existing `@layer base` block):
   ```css
   /* Page transition — subtle fade-in on route change (D-21) */
   @keyframes fade-in {
     from { opacity: 0; }
     to { opacity: 1; }
   }

   .animate-fade-in {
     animation: fade-in 200ms ease-in-out;
   }
   ```

2. Create `src/components/ui/PageTransition.tsx` — Client Component ('use client' required for usePathname):
   - Add `'use client'` directive at the top
   - Import `usePathname` from `next/navigation`
   - Props: `children: React.ReactNode`
   - Render a `<div>` with `key={pathname}` and `className="animate-fade-in"`
   - The `key={pathname}` forces React to unmount/remount the wrapper on every route change, triggering a fresh CSS animation
   - IMPORTANT: This component wraps ONLY the `<main>` content. Header and Footer must be OUTSIDE this wrapper (they persist across routes). This is wired in Plan 03.
   - Add JSDoc comment explaining the key={pathname} mechanism
   - Export as default
  </action>
  <verify>
    <automated>cd /Users/Jennie/Documents/Websites/jennie-slade-photo && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/PageTransition.tsx` exists and contains `'use client'`
    - `src/components/ui/PageTransition.tsx` contains `usePathname` imported from `next/navigation`
    - `src/components/ui/PageTransition.tsx` contains `key={pathname}`
    - `src/components/ui/PageTransition.tsx` contains `animate-fade-in`
    - `src/components/ui/PageTransition.tsx` contains `export default function PageTransition`
    - `src/app/globals.css` contains `@keyframes fade-in`
    - `src/app/globals.css` contains `.animate-fade-in`
    - `src/app/globals.css` contains `animation: fade-in 200ms ease-in-out`
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>PageTransition component exists with usePathname key mechanism. Fade-in keyframe animation is defined in globals.css. Ready to be wired into layout.tsx in Plan 03.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npm run build` completes successfully
- All three component files exist in their correct directories
- No component uses dynamic Tailwind class construction
- No component imports fonts directly (all use .type-* CSS classes)
- No cool gray Tailwind utilities (gray-*) appear anywhere
</verification>

<success_criteria>
- Section, Button, and PageTransition components exist and compile
- Section has three variants using complete Tailwind class strings
- Button uses type-subheading class and has correct border-radius and padding
- PageTransition uses key={pathname} for route-change animation
- globals.css has the fade-in keyframe animation
- lucide-react is installed in node_modules
</success_criteria>

<output>
After completion, create `.planning/phases/02-layout-shell/02-01-SUMMARY.md`
</output>
